{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "env": [
        "NEXTAUTH_URL",
        "NEXTAUTH_SECRET",
        "WALLETCONNECT_PROJECT_ID",
        "CHAIN_ID",
        "LOTTERY_ADDRESS"
      ],
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
